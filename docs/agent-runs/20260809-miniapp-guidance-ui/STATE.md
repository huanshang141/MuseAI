# 小程序内容质量、报告与图片维护检查点

- 目标：修复无意义建议条；去除报告页到访展厅展示；把固定复盘改为可行动指导；为展品增加安全图片上传与无图默认显示；统一小程序 UI；空会话不显示继续游览。
- 仓库边界：后端仅在 `codex/data-driven-miniapp-framework`；小程序仅在 `main`。两仓开始时均干净并与 origin 同步。
- 设计方向：小程序采用“考古田野手账”的 Editorial / Soft Craft 视觉；暖纸色、深墨色、陶土红与克制苔绿，馆藏标签为签名元素。
- 数据边界：建议只来自后端可信展厅/展品结构化字段，不增加运行时 LLM；前端只做质量过滤，不自行补写馆藏事实。
- 兼容边界：报告后端保留既有统计字段，新增指导结构；小程序不展示 `halls_visited`，但后端仍可用于内部计算。
- 图片边界：管理员上传；限制类型、大小与路径；公开 API 返回可访问 URL 或空值；小程序空值/加载失败使用本地默认图。
- 部署基线：服务器 `/home/ubuntu/MuseAI`，2C8G；当前已部署后端提交 `f74c2a9`，生产迁移 head `20260808_remove_legacy_halls`。

## 当前批次

1. 已完成：建议条、报告指导、图片上传/公开读取、CSV `image_url`、事件顺序与 120 字契约实现。
2. 已完成：后端定向测试 197 项；契约修正后相关测试 169 项；最终全量回归 `1182 passed, 23 skipped, 10 warnings`；Ruff、锁文件、依赖和 Alembic head 检查通过。
3. 进行中：最终 diff 复核、提交和推送。
4. 待完成：生产备份、迁移、部署、测试数据重导入与公网 API/图片往返验证。
5. 待完成：本地与服务器文件只读盘点；只列候选，不清理。
